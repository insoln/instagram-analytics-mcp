apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: meta-mcp

resources:
  - namespace.yaml
  - serviceaccount.yaml
  - configmap.yaml
  - redis-statefulset.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  # - hpa.yaml  # uncomment to enable horizontal autoscaling

# secret.example.yaml is documentation only — create the secret manually:
# kubectl -n meta-mcp create secret generic meta-mcp --from-literal=... (see secret.example.yaml)
